{
  "name": "mcp-toolhive",
  "displayName": "ToolHive",
  "description": "Integration for ToolHive",
  "version": "0.0.1-next",
  "icon": "icon.png",
  "publisher": "kortex",
  "license": "Apache-2.0",
  "engines": {
    "kortex": "^0.0.1"
  },
  "main": "./dist/extension.js",
  "scripts": {
    "build": "vite build && node ./scripts/build.js",
    "test": "vitest run --coverage --passWithNoTests",
    "test:watch": "vitest watch --coverage --passWithNoTests",
    "watch": "vite build --watch"
  },
  "dependencies": {
    "@kortex-app/api": "workspace:*"
  },
  "devDependencies": {
    "mkdirp": "^3.0.1",
    "vite": "^7.1.2",
    "vitest": "^3.2.4"
  }
}
